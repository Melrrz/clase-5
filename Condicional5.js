const Dia = Jueves
if (Dia === Lunes or Miercoles) {
    alert ("Historia")
} else {
    if (Dia === Martes) {
        alert ("natacion")
    } else {
        if (Dia === Jueves) {
            alert ("Esgrima")
        } else {
            if (Dia === Viernes or Sabado or Domingo) {
                alert ("Descanso")
            }
        }
    }
}