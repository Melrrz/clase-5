const contraseña = mio
const nombre = pedro 
if (contraseña === mio) {
    if (nombre === pedro){
        alert ("Entras")
    } else {
        alert ("Incorrecto")
    } else {
        alert ("Incorrecto")
    }
}
